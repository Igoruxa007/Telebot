# Telebot
Bot for telegramm

Start project:
- clone repository
- create .env file:
    TOKEN = 'Telegram bot token'
    DATABASE_URL = 'URL to connect to the database'
- install debendencies - `poetry install`
- run `poetry run python mybot/main.py`